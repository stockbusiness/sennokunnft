{
  "name": "@sengoku/integrations",
  "version": "0.1.0",
  "private": true,
  "description": "外部サービスのポート実装（Phase 1 は Fake のみ）",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@sengoku/auth": "workspace:*",
    "@sengoku/config": "workspace:*",
    "@sengoku/domain": "workspace:*",
    "@sengoku/observability": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.20.0",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
