# OPERATIONS_AND_ROLLBACK.md — 運用とロールバック

記法は [README.md](./README.md) に従う。

---

## 1. 環境

🟡 **仮決定:**

| 環境         | 用途         | 外部接続                            |
| ------------ | ------------ | ----------------------------------- |
| `local`      | 開発者の手元 | すべて Fake。実サービスへ接続しない |
| `test`       | 自動テスト   | すべて Fake。ネットワーク遮断       |
| `staging`    | 結合確認     | 決済・認証は各社のテスト環境        |
| `production` | 本番         | 本番サービス                        |

✅ **事実:** 今回（Phase 0-1）は `local` / `test` のみを対象とする。
本番 Stripe / 本番 Supabase / 本番ブロックチェーンへ接続しない。

🟡 **仮決定:** 環境変数 `APP_ENV` で環境を明示し、
**`APP_ENV !== 'production'` のとき、本番ドメインを指す接続先が設定されていたら起動を拒否する**
ガードを入れる。理由: 開発環境から誤って本番に繋ぐ事故が最も重い。

❓ **未決定 `UD-1101`:** ホスティング先と staging の有無（→ `UD-302`）。

---

## 2. 起動と停止

| プロセス | 起動                                  | 停止                                           |
| -------- | ------------------------------------- | ---------------------------------------------- |
| api      | `pnpm --filter @sengoku/api start`    | SIGTERM で処理中リクエストを完了させてから終了 |
| web      | `pnpm --filter @sengoku/web start`    | SIGTERM                                        |
| worker   | `pnpm --filter @sengoku/worker start` | SIGTERM で**現在のジョブ完了後**に終了         |

🟡 **仮決定:** worker は 2 モードを持つ。

| モード       | 用途                                         |
| ------------ | -------------------------------------------- |
| 常駐（既定） | ループでジョブを取得し続ける                 |
| `--once`     | 1バッチだけ処理して終了。cron 起動の環境向け |

理由: デプロイ先（`UD-302`）が常駐プロセスを許すか未定のため、
どちらでも動くようにしておく。

> ⚠️ **worker の SIGTERM 処理は特に重要。**
> `processing` のまま強制終了された行は、外部へ送信済みかどうかが分からず、
> 自動復旧できない（[LAZY_MINT_FLOW.md](./LAZY_MINT_FLOW.md) §3.6）。
> グレースフルシャットダウンでこの状態を作らないようにする。

---

## 3. ヘルスチェック

| エンドポイント | 意味                  | 監視での使い方                   |
| -------------- | --------------------- | -------------------------------- |
| `GET /healthz` | プロセスが生きている  | コンテナの liveness probe        |
| `GET /readyz`  | 依存（DB 等）が使える | ロードバランサの readiness probe |

🟡 **仮決定:** liveness は**外部依存を確認しない**。
DB 障害で liveness が失敗すると、コンテナが再起動を繰り返し、
DB 復旧後の立ち上がりを遅らせる。

---

## 4. 監視とアラート

🟡 **仮決定:** 次を監視する。優先度の高い順。

| 優先 | 指標                                 | 閾値（仮）       | 意味                                       |
| ---- | ------------------------------------ | ---------------- | ------------------------------------------ |
| P1   | `mint.failed` イベント               | 1件でも          | **人手対応が必要。**顧客は支払済みで未受取 |
| P1   | `mint_jobs` の `processing` 滞留時間 | 15分超           | 外部への送信状況が不明。多重発行リスク     |
| P1   | Webhook 署名検証失敗                 | 急増             | 攻撃または設定ミス                         |
| P2   | `mint_jobs` の `queued` 滞留数       | 100件超 / 30分超 | worker 停止またはチェーン障害              |
| P2   | `outbox_events` の未配信滞留         | 30分超           | 配信ワーカー停止                           |
| P2   | 注文の `pending` 滞留数              | 急増             | 決済導線の障害                             |
| P3   | API エラー率 / レイテンシ            | —                | 一般的な劣化検知                           |
| P3   | `readyz` 失敗                        | —                | 依存障害                                   |

> **P1 の3つは「静かに壊れる」種類の障害。**
> 決済は成功しているので売上は立ち、画面も動く。
> 監視がなければ、顧客からの問い合わせで初めて気付く。

> **監視基盤が入るまでのあいだ、`/admin`（運営ダッシュボード）が
> その代わりを務める**（§9）。上の表と同じ性質の異常を、運営が朝いちばんに
> 目で確かめられるようにしてある。⚠️ **これは監視の代替であって、
> 監視ではない。** 見に行かなければ気づけないので、`UD-1102` が決まったら
> アラートへ寄せる。

❓ **未決定 `UD-1102`:** 監視基盤・アラート通知先（Slack / PagerDuty / メール）と
オンコール体制。組織の運用体制が未提示。

---

## 5. 運用手順（Runbook）

### 5.1 Mint が失敗し続ける

```
1. mint.failed の対象 entitlement_id を特定する
2. mint_jobs.last_error_code で分類を確認する
3. 外部プロバイダの状態を確認する
4. 恒久障害でなければ、管理APIから手動再試行
   POST /api/v1/admin/mint-jobs/{id}/retry
   → attempt_count をリセットし status='queued' に戻す
5. 恒久障害なら顧客対応（UD-402 のポリシーに従う。未確定なら事業判断を仰ぐ）
```

### 5.2 ジョブが `processing` のまま滞留

> ⚠️ **自動で `queued` に戻さない。** 二重発行の危険がある。

```
1. MintingPort.getStatus(submissionRef) で外部側の実状態を照会
2. 外部側で「発行済み」→ 手動で nft_tokens を登録し succeeded にする
3. 外部側で「未受付」→ queued に戻して再試行させる
4. 外部側で「不明」→ 戻さない。プロバイダに問い合わせる
5. いずれも mint_jobs.note に対応内容を記録する
```

### 5.3 Webhook を取りこぼした

```
1. 決済事業者のダッシュボードで該当イベントを特定
2. 事業者側の再送機能で再送させる
3. 冪等性（webhook_events の UNIQUE）により、重複しても安全
4. それでも処理されない場合、webhook_events に記録があるか確認
   - 記録あり・status='failed' → アプリのバグ。修正して再送
   - 記録なし → 到達していない。ネットワーク・署名設定を確認
```

### 5.4 提携先（OVEW Wallet）へお届けできていない

管理画面 **「お届け」**（`/admin/wallet-deliveries`）で状態を見る。
必要な権限は閲覧が `integration.view`、送り直しが `wallet_delivery.retry`。

```
1. 「全体の件数」で、打ち切り（DEAD）と失敗（FAILED）の数を見る
   ⚠️ 絞り込みを掛けても、この件数は全体を指す
2. 行を開き、「うまくいかなかった理由」を読む
   - 提携先に断られました（401 / 403） → 資格情報。/admin/integrations を見る
   - 提携先で問題が起きています（5xx） → 相手の復旧待ち。しばらく置いて送り直す
   - お届けの内容を受け付けてもらえませんでした（4xx） → こちらの不具合。
     直してから送り直す。同じ内容を送り直しても直らない
   - 時間内に応答がありませんでした / 接続できませんでした → 経路。復旧後に送り直す
3. 直ったら「もう一度お届けする」を押す
```

⚠️ **「お届けしています」（PROCESSING）の行は送り直せない。**
送信中か、送信直後に落ちた可能性がある。届いたかどうかが分からない状態で
押し直すと、相手の冪等性だけが最後の砦になる。しばらく置くと、
取り残しの回収（`reclaimStale`）で `PENDING` に戻り、自動で送り直される。

⚠️ **お届けした本文は画面に出ない。** お客さまの情報が含まれるため
（指示書 §5）。提携先へ問い合わせるときは「お問い合わせ番号」（`event_id`、
相手の `Idempotency-Key` と同じ値）と「調査用の番号」（`correlation_id`）を伝える。

管理画面が開けないとき（配備の事故・認証の不調）は、CLI が逃げ道として残っている:

```
pnpm wallet:resend --event-id=<お問い合わせ番号>
```

### 5.5 提携先の接続先や鍵を替えたい

管理画面 **「外部サービス」**（`/admin/integrations`）。**オーナーだけ**が操作できる。

```
1. 画面の先頭で staging / production のどちらを触っているか確かめる
2. 「接続先を設定する」に URL と鍵の名前（鍵ID）を入れて保存
   ⚠️ 接続先を変えると、それまでの接続確認の成功は無効になる
3. 「鍵をお預かりする」に新しい鍵を貼り付けて保存
   → この時点では**まだ使われない**（待機中）
4. 「接続を確かめる」を押す
5. 「お預かりしている鍵」の一覧から「この鍵を使いはじめる」を押す
   → 古い鍵はここで退役する
6. 「お届けを始める」を押す
```

⚠️ **鍵は交換の途中で 2 本になったり 0 本になったりしない。**
「使いはじめる」は、新しい鍵の有効化と古い鍵の退役を 1 つの書き込みで行う。

⚠️ **worker の再起動は要らない。** 送信のたびに DB から接続先と鍵を読む
（要決定 03）。再起動を前提にすると、忘れたときに「古い鍵で送り続けている」
ことに誰も気づけない。

⚠️ **「接続を確かめる」で分かるのは、接続先へ届くことまで。**
鍵が正しいかどうかは分からない。先方に安全なテスト手段があるかを
伺えていないため、業務データを送る確認は作っていない（要決定 06）。

⚠️ **管理画面で接続先を入れると、以後は DB が正になる。**
それまで `WALLET_DELIVERY_*` の環境変数で動いていた環境も、
接続先を保存した時点で DB を見るようになる。**「お届けを止める」は
環境変数へ落ちない**——止めたのに送り続ける、を作らないため。

### 5.6 画像が出ない・ログインできない

管理画面 **「外部サービス」** から `画像の保管先` / `ログイン` を開く。
**この 2 つは、この画面からは変更できない**（配備環境の設定が正）。
見られるのは次のとおり。

```
1. 「使っている方式」を確かめる（r2 / local、supabase / dev）
2. 「設定が足りません」と出ていれば、足りない設定の名前が並ぶ
   → 配備環境（Vercel / Fly）に設定して入れ直す
3. 「接続を確かめる」で、公開されている場所へ届くかを見る
   → 画像なら配信元、ログインなら鍵束の置き場
```

⚠️ **確かめられるのは到達性まで。** 保管先への書き込みや、ログインの検証が
うまくいくかどうかは分からない。

⚠️ **ログインの設定をこの画面に置いていないのは、意図的。**
誤ると全員が入れなくなり、直す手段がログインの先にある。

### 5.7 誰が何をしたのかを調べたい

管理画面 **「操作の記録」**（`/admin/audit-logs`）。必要な権限は `audit_log.view`
（`operator` と `auditor`）。「操作の種類」に `staff` や `integration` のように
先頭を入れて絞り込む。

⚠️ **メールアドレスはオーナー以外には伏せて表示される。**
記録そのものは残っているので、必要なときはオーナーが確認する。

⚠️ **この画面から記録を消したり直したりはできない。** 経路そのものが無い。

### 5.8 在庫（発行上限）の不整合を疑うとき

```sql
-- reserved_count が実際の pending 注文と一致するか
SELECT a.id, a.reserved_count,
       COALESCE(SUM(ol.quantity), 0) AS actual_reserved
  FROM artworks a
  LEFT JOIN order_lines ol ON ol.artwork_id = a.id
  LEFT JOIN orders o ON o.id = ol.order_id AND o.status = 'pending'
 GROUP BY a.id
HAVING a.reserved_count <> COALESCE(SUM(ol.quantity), 0);

-- issued_count が entitlements 件数と一致するか
SELECT a.id, a.issued_count, COUNT(e.id) AS actual_issued
  FROM artworks a LEFT JOIN entitlements e ON e.artwork_id = a.id
 GROUP BY a.id HAVING a.issued_count <> COUNT(e.id);
```

🟡 **仮決定:** この整合性チェックを**日次で自動実行**し、
不一致があればアラートする。カウンタのずれは静かに進行するため。

---

## 6. デプロイ

🟡 **仮決定:**

| 原則         | 内容                                                                        |
| ------------ | --------------------------------------------------------------------------- |
| 順序         | DB マイグレーション → api → worker → web                                    |
| 互換性       | マイグレーションは**後方互換**（expand → migrate → contract）               |
| worker       | 新旧が同時に動いても壊れないこと（ジョブの取得は排他制御済み）              |
| ロールバック | アプリは前バージョンへ戻せる。**DB は前方向のみ**（データ損失を避けるため） |

> ⚠️ **DB を戻す前提のデプロイをしない。** 発行記録は再現不能な資産である。
> 「戻せないマイグレーション」を打つ前に、必ずバックアップと復旧手順を確認する。

❓ **未決定 `UD-1103`:** バックアップの取得間隔・保持期間・復旧目標（RPO/RTO）。

---

## 7. Phase 1 のロールバック条件

✅ **事実:** 次のいずれかに該当する場合、Phase 1 を**マージしない**。

| #   | 条件                                             | 本 Phase での担保                                                                                       |
| --- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------- |
| 1   | CI が失敗する                                    | GitHub Actions で lint / typecheck / test / build を全実行                                              |
| 2   | 秘密情報がコミットされている                     | `check:secrets` スクリプトを CI に組み込み                                                              |
| 3   | web / api / worker の境界が不明確                | 各アプリを独立パッケージ化。責務を [ARCHITECTURE.md](./ARCHITECTURE.md) §3 に明記                       |
| 4   | ビジネスロジックがフレームワーク層へ混在している | `domain` は NestJS / Next.js に依存しない。`check:deps` で機械検査                                      |
| 5   | 本番サービスへ接続している                       | 実アダプタを実装せず Fake のみ。テストで外部通信を遮断                                                  |
| 6   | 未決定のブロックチェーン仕様を勝手に確定している | [BLOCKCHAIN_DECISION_RECORD.md](./BLOCKCHAIN_DECISION_RECORD.md) に ADR 0件と明記。識別子は不透明文字列 |

---

## 7.1 全額返金にともなう取り消し（M3a）

### フラグ（3 本）

⚠️ **1 本にまとめない。** 止めたい対象が 3 段あるため。まとめると、
送信だけ止めたい場面で生成まで止まり、**止めていた期間の返金が永久に
相手へ伝わらなくなる**。

| 環境変数                                     | 役割                                             | 既定    |
| -------------------------------------------- | ------------------------------------------------ | ------- |
| `REFUND_REVOKE_CLAIMED_ENABLED`              | `claimed → revoked` を行うか                     | `false` |
| `WALLET_REVOCATION_EVENT_GENERATION_ENABLED` | 取消イベントを**作る**か（**日次の補完も従う**） | `false` |
| `WALLET_REVOCATION_EVENT_DELIVERY_ENABLED`   | 取消イベントを**送る**か                         | `false` |

- `"true"` の完全一致だけを有効とする。`"1"` や `"TRUE"` は**黙って無効に
  倒さず起動を拒否する**（綴り違いで「有効にしたつもりが無効」を防ぐ）。
- 配送には既存の `WALLET_DELIVERY_ENABLED`（ワーカーの親スイッチ）も要る。
  取消の配送だけ有効にすると**起動時に拒否**する（1 件も送られないため）。
- 取消の配送を止めても、**付与の配送は止まらない**。

### 通常のロールバック（データを消さない）

| 段  | 操作                                               | 効果                                                             |
| --- | -------------------------------------------------- | ---------------------------------------------------------------- |
| 1   | `WALLET_REVOCATION_EVENT_DELIVERY_ENABLED=false`   | 送信のみ停止。行は残り、復旧後に再送できる                       |
| 2   | `WALLET_REVOCATION_EVENT_GENERATION_ENABLED=false` | 生成停止（**日次の補完も止まる**）。取りこぼしは後述の補完で回収 |
| 3   | `REFUND_REVOKE_CLAIMED_ENABLED=false`              | `claimed → revoked` を停止。**返金自体は従来どおり完了する**     |
| 4   | コードを revert                                    | 1〜3 で足りない場合のみ                                          |

### ⚠️ DB 制約は戻さない

**自動ロールバック手順に「CHECK 制約を旧状態へ戻す」操作を含めない。**
`claimed → revoked` の行が 1 件でもできた後は、`ADD CONSTRAINT` が既存行の
検証で失敗し、**ロールバック自体が途中で止まる**。

どうしても戻す必要が生じた場合は、**既存データの扱いを含む個別の移行計画**
として別途承認を得る（データの削除・null 化を伴うため）。

### 後戻りの境目は 2 段階

|          | 境目                                              | 発生条件                                                       | 戻せなくなるもの                                  |
| -------- | ------------------------------------------------- | -------------------------------------------------------------- | ------------------------------------------------- |
| **第 1** | `claimed → revoked` が本番で **1 件発生**した時点 | `REFUND_REVOKE_CLAIMED_ENABLED` を有効にした後の最初の全額返金 | **CHECK 制約を旧状態へ単純に戻せない**            |
| **第 2** | 取消イベントが Wallet へ **1 件配送**された時点   | 配送フラグを有効にした後                                       | **Market だけ戻しても Wallet 側に取消状態が残る** |

⚠️ **有効化の Go 承認時に、この 2 段階を読み合わせる。**

### 取りこぼしの補完

生成フラグが無効だった期間に全額返金が起きると、取消イベントは作られない。
あとから拾い直す。

```
# 数えるだけ（既定）
pnpm wallet:revocation-backfill

# 実際に積む。⚠️ 生成フラグが有効でなければ拒否する
pnpm wallet:revocation-backfill --execute --limit=200
```

⚠️ **補完はフラグを迂回しない。** 先に生成フラグを有効にしてから実行する。
「止めたはずのものが別の入口から動く」状態を作らないため。

日次の `POST /api/v1/internal/jobs/reconcile-revocations` も同じ処理を行い、
**同じく生成フラグに従う**。

### 適用順序（DB 先行）

⚠️ **マイグレーションを、コードのデプロイより先に当てる。** 逆にすると、
新コードが本番に出ているのに enum も新制約も無い時間が生まれる。

| 環境          | 順序                                                                                                                                                            |
| ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ステージング  | ①事前検証 → ②マイグレーション適用 → ③コードをデプロイ → ④3 フラグ OFF で起動確認 → ⑤Wallet 側の契約テスト（`event_version=1.1` / `reason_code`）→ ⑥レビュー承認 |
| 本番          | ①事前検証 → ②**マイグレーション適用（コードより先）** → ③旧コードが新制約・新 enum で動くことを確認 → ④マージ → ⑤デプロイ → ⑥3 フラグ OFF を確認                |
| 後日（別 Go） | ⑦`REFUND_REVOKE_CLAIMED_ENABLED` → ⑧生成フラグ → ⑨補完を実行 → ⑩配送フラグ                                                                                      |

⚠️ **マイグレーション適用と本番フラグ変更を同じ作業にしない。**

### 監視

| 見るもの                                                 | 意味                                                                                                |
| -------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| `operations_reviews` の未対応件数                        | 機械が決められなかったことが溜まっていないか                                                        |
| `wallet_delivery_outbox` の `SUPERSEDED` 件数            | 取消に追い越された付与。異常ではない                                                                |
| `REFUND_REVOKE_CLAIMED_ENABLED=true` かつ生成 OFF の期間 | ⚠️ **Wallet との不整合が溜まる。** 長く置かない。手順⑦〜⑧は同じ作業枠で速やかに行い、補完で回収する |

---

## 8. リポジトリ

✅ **事実:** 本プロジェクトの正式なリポジトリは `stockbusiness/sennokunnft`。
既存システムのリポジトリとは分離しており、コードの流用は行わない。

Phase 0-1 の実装は、本リポジトリが用意される前は既存リポジトリ
`stockbusiness/sengokumarket` のサブディレクトリで進められた。
その内容は `git subtree split` で**コミット履歴を保持したまま**移送済みである。

> ⚠️ 移送元にコードを残さないこと。
> 2 箇所に同じコードがあると、必ず片方が古くなる。

---

## 9. Phase 0-1 検証結果

実行環境: Node.js v22.22.2 / pnpm 9.15.4 / Linux x86_64 / 実行日 2026-08-13

すべて✅合格。詳細は下表のとおり。

| #   | 検査項目                       | コマンド                         | 結果                                                      |
| --- | ------------------------------ | -------------------------------- | --------------------------------------------------------- |
| 1   | クリーン環境で依存導入         | `pnpm install`                   | ✅ 12 ワークスペース、414 パッケージを導入                |
| 2   | Lint                           | `pnpm lint`                      | ✅ 12/12 パッケージで指摘なし                             |
| 3   | 型検査                         | `pnpm typecheck`                 | ✅ 21 タスク成功（`strict` + `noUncheckedIndexedAccess`） |
| 4   | 単体テスト                     | `pnpm test`                      | ✅ 16 ファイル / **272 テスト全通過**                     |
| 5   | ビルド                         | `pnpm build`                     | ✅ 12 タスク成功（web は Next.js の本番ビルドまで）       |
| 6   | 依存グラフ検査                 | `pnpm check:deps`                | ✅ 循環なし・層越えなし                                   |
| 7   | 秘密情報検査                   | `pnpm check:secrets`             | ✅ `.env` 混入なし / `.env.example` に値なし / 直書きなし |
| 7b  | 未決定事項レジスタ検査         | `pnpm check:docs`                | ✅ 45件、重複・未登録・件数不一致なし                     |
| 8   | Prisma Client 生成             | `pnpm db:generate`               | ✅ `DATABASE_URL` 未設定でも生成できることを確認          |
| 9   | api 起動＋ヘルスチェック       | `curl /healthz` `curl /readyz`   | ✅ ともに 200。応答本文は契約スキーマに適合               |
| 10  | api のグレースフル停止         | `kill -TERM`                     | ✅ 終了コード 0、停止ログを出力                           |
| 11  | 構造化ログ                     | 起動ログの検査                   | ✅ フレームワークの起動ログを含め**全行が JSON**          |
| 12  | worker 起動（常駐 / 1回実行）  | `node dist/main.js --once`       | ✅ 終了コード 0                                           |
| 13  | 環境変数不足時の安全な起動失敗 | 変数を外して api / worker を起動 | ✅ 終了コード 1。**変数名のみ出力し、値を出さない**       |
| 14  | 実サービス接続の拒否           | `MINT_PROVIDER=ethereum` で起動  | ✅ 起動を拒否（終了コード 1）                             |
| 15  | E2E スモーク                   | `pnpm e2e`                       | ✅ 3 テスト通過（外部通信の遮断つき）                     |
| 16  | 整形                           | `pnpm format:check`              | ✅ 全ファイルが Prettier 準拠                             |

### 補足

- **13 の出力例**（値を含まないことの確認）:

  ```
  環境変数の検証に失敗しました。以下を設定してください:
    - DATABASE_URL: missing or wrong type
  ```

- **9 の応答例**:

  ```json
  { "status": "ok", "service": "api", "version": "0.1.0", "uptimeSec": 0 }
  { "status": "ok", "service": "api", "checks": [] }
  ```

- **11 の出力例**（フレームワークのログも同じ経路を通る）:

  ```json
  {"level":"info","time":"...","service":"api","nestContext":"NestApplication","msg":"Nest application successfully started"}
  {"level":"info","time":"...","service":"api","port":3903,"appEnv":"local","version":"0.1.0","msg":"api started"}
  ```

### 検証中に見つかり、修正した問題

| 事象                         | 原因                                                                                     | 対応                                                                                 |
| ---------------------------- | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| 起動ログが出力されない       | pino の既定バッファリングにより、起動直後・終了直前のログが失われていた                  | 標準出力への書き込みを同期に変更（障害調査でいちばん必要な区間を取りこぼさないため） |
| SIGTERM の終了コードが 143   | NestJS の `enableShutdownHooks()` と自前のシグナルハンドラが二重に停止処理を走らせていた | 自前のハンドラに一本化し、正常停止を 0 で返すようにした                              |
| フレームワークのログだけ平文 | NestJS 内部ログが独自ロガーを使っており、マスキングを経由していなかった                  | `NestStructuredLogger` で共通ロガーへ集約（マスキングを素通りする出口をなくす）      |

### 既知の制約

- `prisma validate` は `DATABASE_URL` が必要（`prisma generate` は不要）。
  CI では実行していない。
- E2E は、ブラウザが事前配置された環境ではバージョン不一致で起動できないことがある。
  その場合は `PLAYWRIGHT_CHROMIUM_EXECUTABLE` で実行ファイルを指定する。
  CI は `playwright install` で正規のブラウザを取得するため、この制約に当たらない。
- 実 PostgreSQL に対する結合テスト（DB 制約の実動作検証）は Phase 2 で実施する。
  Phase 1 ではスキーマ上に制約が定義されていることを静的に検査するに留めている。

---

## 9-2. Phase 2 検証結果

実行環境: Node.js v22.22.2 / pnpm 9.15.4 / PostgreSQL 16.13 / 実行日 2026-08-13

| #   | 検査項目                                 | 結果                                                         |
| --- | ---------------------------------------- | ------------------------------------------------------------ |
| 1   | `pnpm verify`（全 48 タスク）            | ✅ 成功                                                      |
| 2   | 単体・結合テスト                         | ✅ **375 テスト全通過**（Phase 1 の 272 から +103）          |
| 3   | 実 PostgreSQL に対する制約の結合テスト   | ✅ 14 件。CHECK 制約・UNIQUE 制約が実際に効くことを確認      |
| 4   | オーバーセルの最終防壁                   | ✅ 上限超過・負の在庫・同時更新をいずれも DB が拒否          |
| 5   | マイグレーション適用                     | ✅ `prisma migrate deploy` で 18 個の CHECK 制約を含めて適用 |
| 6   | 認可（未認証・buyer・auditor・operator） | ✅ 権限マトリクスどおりに 401 / 403 / 200                    |
| 7   | 初回アクセス時のロール                   | ✅ 常に `buyer`。トークンのクレームでは昇格できない          |
| 8   | 管理APIの一連の操作                      | ✅ 登録 → 公開 → 出品 → 販売開始まで実 DB で通し確認         |
| 9   | 公開カタログ（未認証）                   | ✅ 公開作品のみを返す。未公開は 404                          |
| 10  | 画面（一覧・詳細）                       | ✅ 実データを表示。存在しない作品は 404                      |
| 11  | API 停止時の画面                         | ✅ 画面は落ちず、案内を表示。**404 と断定しない**            |
| 12  | 未決定事項レジスタ                       | ✅ 46 件、重複・未登録・件数不一致なし                       |
| 13  | `format:check`                           | ✅ 全ファイルが Prettier 準拠                                |

### 検証中に見つけて対処した問題

| 事象                                               | 対応                                                                              |
| -------------------------------------------------- | --------------------------------------------------------------------------------- |
| ヘルスチェックがガードに阻まれて 401               | 既定 deny が正しく働いた結果。プローブは資格情報を持たないため `@Public()` を付与 |
| ドメインエラーの HTTP 対応表に新コードが未登録     | 型検査が検出。既存テストが全コードの網羅を要求していたため漏れなく追加できた      |
| コードコメントに書いた `UD-205` がレジスタに未登録 | `check:docs` の検査範囲をソースまで広げ、登録した                                 |
| 結合テストのハーネスが `process.env` 制限に抵触    | テスト配下のみ制限を解除（本体コードには引き続き適用）                            |
| API 停止時に作品詳細を 404 にしていた              | 一時障害を「存在しない」と断定しないよう変更。E2E で固定                          |

### 既知の制約

- **管理画面（UI）は未実装。** 運営のログイン経路が Phase 4 のため。
  管理APIは実装済みで、テストと手元から操作できる。
- ロール変更はアプリからは行えない。DB 直操作＋監査ログで運用する（`UD-803`）。
- 一覧取得で作品ごとに出品を引くため N+1 になる。1 ページ最大 100 件で
  実用上問題ない範囲に収めており、必要になってからまとめ取得へ差し替える。

---

## 9-3. Phase 2（指示書対応）検証結果

実行環境: Node.js v22.22.2 / pnpm 9.15.4 / PostgreSQL 16.13 / 実行日 2026-08-13

| #   | 検査項目                                    | 結果                                                 |
| --- | ------------------------------------------- | ---------------------------------------------------- |
| 1   | `pnpm install --frozen-lockfile`            | ✅                                                   |
| 2   | `pnpm verify`（48 タスク）                  | ✅                                                   |
| 3   | 単体・結合テスト                            | ✅ **470 テスト全通過**                              |
| 4   | `pnpm format:check`                         | ✅                                                   |
| 5   | `pnpm db:generate`                          | ✅                                                   |
| 6   | 空の PostgreSQL 16 へのマイグレーション適用 | ✅ CHECK 21 個・部分ユニーク索引・トリガを含めて適用 |
| 7   | DB 結合テスト                               | ✅ 26 件。制約が実際に効くことを確認（下記参照）     |
| 8   | E2E                                         | ✅ 12 件（通しシナリオ 2 件を含む）                  |
| 9   | 管理者が作品を登録・公開・出品できる        | ✅ E2E の通しシナリオで確認                          |
| 10  | 一般ユーザーが販売中作品を閲覧できる        | ✅ 一覧・詳細ともに実データを表示                    |
| 11  | 非公開作品が公開APIへ出ない                 | ✅ 一覧・詳細・出品APIすべてで確認                   |
| 12  | 画像アップロードの検証                      | ✅ SVG・HTML・実行ファイル・偽装・サイズ超過を拒否   |
| 13  | 管理APIが認可で保護されている               | ✅ 未認証 401／buyer 403／auditor は閲覧のみ         |
| 14  | 未決定事項レジスタの整合性                  | ✅ 46 件、重複・未登録・件数不一致なし               |

### DB 制約（21 個の CHECK ＋ 索引 ＋ トリガ）

指示書が求めた保証は、すべて DB 側で担保している。

| 要求                                     | 実現方法                                                                                        |
| ---------------------------------------- | ----------------------------------------------------------------------------------------------- |
| `max_supply >= 1`                        | CHECK `artworks_max_supply_positive`                                                            |
| `reserved_count >= 0`                    | CHECK `artworks_reserved_count_non_negative`                                                    |
| `issued_count >= 0`                      | CHECK `artworks_issued_count_non_negative`                                                      |
| `reserved + issued <= max_supply`        | CHECK `artworks_supply_within_max`                                                              |
| 販売価格は 0 円より大きい                | CHECK `listings_price_positive`                                                                 |
| 終了日時は開始日時より後                 | CHECK `listings_period_ordered`                                                                 |
| slug は一意                              | UNIQUE `artworks.slug`                                                                          |
| 公開済み作品だけを出品できる             | **トリガ** `listings_require_published_artwork`（別テーブル参照のため CHECK では表現できない）  |
| 同一作品に同時に複数の有効出品を作れない | **部分ユニーク索引** `listings_one_effective_per_artwork`（`status IN ('active','scheduled')`） |

### 検証中に見つけて対処した問題

| 事象                                                                            | 対応                                                                                                                                                                                    |
| ------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `evaluatePurchasability` が「開始日時を過ぎた `scheduled`」を購入不可にしていた | 「バッチを前提にしない」という設計の要点を自分で潰していた。分岐を削除し、DB の CHECK で不整合を防ぐ形に整理                                                                            |
| 販売終了後の画面が「ただいま販売しておりません」になっていた                    | `resolveDisplayState` が状態 `ended` を写していなかった。再開を待たせてしまうため、終了は終了として伝えるよう修正                                                                       |
| Prisma が一意制約違反を独自メッセージに変換し、制約名で判定できなかった         | 一意制約用の判定ヘルパーを分離。CHECK 制約は従来どおり制約名で確認                                                                                                                      |
| ESLint の `no-control-regex` が制御文字検査に抵触                               | 制御文字の検出そのものが目的なので、理由を書いて当該行のみ無効化                                                                                                                        |
| **E2E が保存した画像 9 件をリポジトリへ取り込んでいた**                         | `MEDIA_STORAGE_DIR` の保存先を `.gitignore` に入れていなかった。追跡を外し、`.media/` と `.e2e-media/` を除外。**アップロードされたデータをリポジトリへ入れない**という前提が抜けていた |
| **DB 結合テスト 26 件が `pnpm verify` と CI で黙って飛んでいた**                | 下記「番人が届かなかった件」を参照                                                                                                                                                      |

#### 番人が届かなかった件（最も重い見落とし）

**事象:** DB 結合テスト 26 件が、`pnpm verify` でも CI でも実行されていなかった。
「制約が実際に効くことを確認済み」という報告は、**この時点では裏付けが無かった**。

**原因:** Turborepo 2 は既定で環境変数を絞り込み、`turbo.json` に書いた変数しか
タスクへ渡さない。`TEST_DATABASE_URL` を書いていなかったため接続先が届かず、
結合テストはスキップされていた。

**なぜ気付けなかったか:** スキップを失敗に変えるための番人
（`REQUIRE_INTEGRATION_TESTS=1`）を用意してあったが、**その番人を起動する変数自体が
同じ絞り込みで消えていた。** 合図が届かなければ、合図を頼りにした番人は何もしない。
CI は緑で、テスト件数も増え続けていたので、外からは正常に見えていた。

**対処:**

1. `turbo.json` の `test` タスクへ `DATABASE_URL` / `TEST_DATABASE_URL` /
   `REQUIRE_INTEGRATION_TESTS` を明記した
2. 番人の判定を `REQUIRE_INTEGRATION_TESTS=1` **または** `CI` に広げた。
   片方の合図が消えても止まる（`CI` は `globalEnv` にあり必ず届く）
3. 接続先を外した状態で実際に失敗することを確認した

**教訓:** 「検査を必須にする仕組み」自体が、検査対象と同じ経路で壊れることがある。
番人を置いたら、**番人が実際に噛みつくところまで確かめる。**
テスト件数が 444 → 470 に増えたのは、機能追加ではなく、飛んでいた 26 件が戻ったため。

### 既知の制約

- **画面からの登録・編集フォームは未実装。** 運営のログイン経路が Phase 4 のため、
  現時点では画面に API の手順を案内している。押せるが何も起きないボタンは置いていない。
- ~~冪等キーの保存先はプロセス内メモリ~~ → **9-4 で DB へ移した。**
- ~~作品を `archived` にしても既存の出品は残る~~ → **9-4 で塞いだ。**
- 一覧取得で作品ごとに出品を引くため N+1 になる。1 ページ最大 100 件で
  実用上問題ない範囲に収めており、必要になってからまとめ取得へ差し替える。

---

## 9-4. Phase 3 の前提づくり（保証を手順から構造へ）

実行環境: Node.js v22.22.2 / pnpm 9.15.4 / PostgreSQL 16.14 / 実行日 2026-08-13

決済事業者（`UD-702`）や税（`UD-401`）が未決定でも進められる部分を先に片付けた。
どちらも Phase 2 で「運用でカバーする」「Phase 3 で移す」と書いて先送りしていたもので、
**先送りの理由が「今はまだ壊れていない」だったため、壊れる前に閉じた。**

### 1. 「非公開なのに販売中の出品がある」を作れなくした

Phase 2 では出品側にトリガを置き、非公開の作品に有効な出品を**作れない**ようにしていた。
しかし作品側には何も無く、`published → archived` にするだけで
**有効な出品が非公開の作品を指す状態が成立していた。**
トリガが防ごうとしていた状態そのものを、別の入口から作れていたことになる。

実 PostgreSQL で到達可能なことを確認したうえで、次の 3 段にした。

| 段  | 何をするか                                                                | 位置づけ           |
| --- | ------------------------------------------------------------------------- | ------------------ |
| 1   | ドメイン関数が作品と出品の遷移をまとめて決める                            | 正しい手順を楽に   |
| 2   | リポジトリが**同一トランザクション**で書く                                | 途中で落ちない     |
| 3   | トリガ `artworks_no_effective_listings_when_unpublished` が他の経路を拒否 | **保証はここだけ** |

マイグレーションでは、既に成立してしまっている行を先に修復してからトリガを張る。
トリガは既存行を検証しないため、これを飛ばすと
「制約はあるのに違反した行がある」状態が残る。

**制約は「作れないこと」だけでなく「残らないこと」まで見ないと守れない。**

### 2. 冪等キーを DB へ移し、「先に占有する」方式にした

保存先の変更（メモリ → DB）だけでなく、**判定の順序**を変えた。

| 方式                      | 同時に 2 本来たとき                  |
| ------------------------- | ------------------------------------ |
| 探す → 無い → 実行（旧）  | 両方が「無い」を見て**両方実行する** |
| 先に占有 → 取れた側が実行 | 取れるのは 1 本だけ                  |

読み取りと書き込みのあいだの隙間は `if` では塞げない。占有は一意制約が決める。

実 PostgreSQL に対して 8 本同時に取り合わせ、占有できたのが 1 本だけであることを確認した。

失敗時の扱いは非対称にしてある。

- **本体が失敗したら解放する。** 解放しないと、一度失敗しただけのキーが
  期限切れまで塞がり、利用者がやり直せなくなる
- **結果の記録に失敗しても解放しない。** 本体は成功しているので、解放すると
  やり直しで本体がもう一度走る。塞がったままなら「やり直せない」で済むが、
  解放すると「二重に実行される」

### 検証結果

| #   | 検査項目                         | 結果                                          |
| --- | -------------------------------- | --------------------------------------------- |
| 1   | `pnpm verify`（48 タスク）       | ✅                                            |
| 2   | 単体・結合テスト                 | ✅ **502 テスト全通過**（470 から +32）       |
| 3   | DB 結合テスト                    | ✅ 44 件（26 から +18）                       |
| 4   | 非公開化の穴が実際に塞がったこと | ✅ 逆順のトランザクションが巻き戻ることも確認 |
| 5   | 冪等キーの同時占有               | ✅ 8 本同時で占有は 1 本のみ                  |
| 6   | E2E                              | ✅ 12 件                                      |
| 7   | `format:check` / `check:*`       | ✅                                            |

### 検証中に見つけて対処した問題

| 事象                                                                                               | 対応                                                                                                                    |
| -------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| テスト用の冪等キーが即座に期限切れになった                                                         | 保管庫が実時刻を、サービスが固定時計を見ていた。**実装が自分で時計を読むのをやめ**、呼び出し側から `now` を渡す形にした |
| `created_at` の既定（DB の時計）と `expires_at`（呼び出し側の時計）を比べる CHECK が成立しなかった | 行の時刻を 1 つの時計から入れるようにした。別々の時計で書かれた 2 つの時刻を比べても意味がない                          |
| 未知の状態を入れたとき、別の CHECK が先に反応した                                                  | 制約 1 つにつき役割 1 つになるよう整理した。どの規則を破ったのかが分からないと、運用時に直しようがない                  |
| `scheduled` の出品を作るテストが CHECK に弾かれた                                                  | `listings_scheduled_requires_start` が正しく機能していた。テスト側の作り方が誤っていたので直した                        |

### 積み残し

- 期限切れレコードの掃除は、次に同じキーが来たときに行う。
  誰も来なければ残り続けるので、定期削除は運用設計（`UD-1102` と併せて）で決める。
- 冪等キーの必須化はまだしていない。カタログ操作は作り直せるため任意のまま。
  取り返しのつかない注文（Phase 3）で必須にする。

---

## 9-5. 共通顧客ID 連携（PR-NW02）検証結果

実行環境: Node.js v22.22.2 / pnpm 9.15.4 / PostgreSQL 16.14 / 実行日 2026-08-13

| #   | 検査項目                                 | 結果                           |
| --- | ---------------------------------------- | ------------------------------ |
| 1   | `pnpm verify`（48 タスク）               | ✅                             |
| 2   | 単体・結合テスト                         | ✅ **525 テスト全通過**        |
| 3   | 外部が落ちていても例外を投げない         | ✅ 購入・認証を止めない        |
| 4   | 4xx は再試行せず人手へ回す               | ✅                             |
| 5   | timeout / 5xx は時間をおいて再試行       | ✅ 指数バックオフ、上限 5 回   |
| 6   | 既存と異なる値で自動上書きしない         | ✅ `CONFLICT` で停止し値を保持 |
| 7   | 未検証の属性での一致を受け入れない       | ✅ `identity:*` は `CONFLICT`  |
| 8   | 同時実行で古い結果が新しい結果を潰さない | ✅ 条件付き UPDATE             |
| 9   | DB 制約が実際に効く                      | ✅ 形式・状態・整合性の 7 件   |
| 10  | 有効なのに設定不足なら起動を拒否         | ✅                             |
| 11  | `format:check` / `check:*`               | ✅                             |

### 運用上の注意

⚠️ **`create_if_missing` の既定値は相手側で `true`。**
照会のつもりで省略すると、副作用として人物が新規作成される。
本システムのポートでは**省略できない必須項目**にしてある。

⚠️ **`CONFLICT` と `ERROR` は自動で解けない。**
ワーカーは件数をログに出すだけで、値は触らない。
`common_user_id` そのものはログに出さない。

### 積み残し

- ログイン直後のベストエフォート解決は入れていない。
  指示書 §5.3 は「試してもよい」（任意）としており、
  認証経路に外部呼び出しを足すと**遅延と失敗の面が増える**ため、
  まずワーカー側だけで運用して様子を見る。
- 掃き出しの間隔は `WORKER_POLL_INTERVAL_MS` に従う。
  常駐と cron のどちらで動かすかは `UD-1101` のまま未決定で、
  **どちらでも動く形**にしてある。

---

## 9-6. Claim API（PR-NW03）検証結果

実行環境: Node.js v22.22.2 / pnpm 9.15.4 / PostgreSQL 16.14 / 実行日 2026-08-14

⚠️ **本節は「本システム側の検証結果」であり、両システム間の疎通確認ではない。**
指示書 §12 の着手条件のうち **10（Wallet側HMAC修正済み）と 11（固定HMAC vector両側PASS）は
OVEW Wallet 側の作業であり、未完了**。したがって staging 結合（回答書 §10 手順8）には進めない。
Feature Flag は**既定 OFF** のままとする。

| #   | 検査項目                                               | 結果                                               |
| --- | ------------------------------------------------------ | -------------------------------------------------- |
| 1   | 署名が無い要求を拒否する                               | ✅ 401                                             |
| 2   | 署名が違う要求を拒否する                               | ✅ 403                                             |
| 3   | 未知の鍵IDを拒否する                                   | ✅ 403                                             |
| 4   | タイムスタンプの許容範囲を検証する                     | ✅ 403                                             |
| 5   | nonce の再利用を拒否する                               | ✅ 2 回目が 403                                    |
| 6   | **生の本文で検証している**                             | ✅ 本文を変えると 403                              |
| 7   | 失敗理由を応答に含めない                               | ✅ 本文に nonce/timestamp/key/signature を含まない |
| 8   | Flag が無効なら存在ごと隠す                            | ✅ 404                                             |
| 9   | 有効なのに鍵が無ければ起動を拒否する                   | ✅ 拒否理由に秘密鍵を含めないことも確認            |
| 10  | `GET` の応答が status / card_name / expires_at のみ    | ✅ 画像・シリアルを返さない                        |
| 11  | 受理は `202` + `DELIVERY_PENDING`                      | ✅                                                 |
| 12  | 未解決は `202` + `PENDING` + `common_user_pending`     | ✅ 失効させない                                    |
| 13  | 別人は `409 COMMON_USER_MISMATCH`                      | ✅                                                 |
| 14  | 期限切れは `410 CLAIM_EXPIRED`                         | ✅ 状態・時刻経過の両方                            |
| 15  | 取り消しは `409 CLAIM_REVOKED`                         | ✅                                                 |
| 16  | エラーが全 API 共通の封筒                              | ✅ Claim だけ独自形式にしない                      |
| 17  | 公開状態が 5 値の外へ出ない                            | ✅ 内部 4 × 配送 3 の全組合せで確認                |
| 18  | **同時に確定できるのは 1 本だけ**                      | ✅ 8 本同時で確定は 1 本（実 PostgreSQL）          |
| 19  | 受け取っていないものを配送状態にできない               | ✅ CHECK が発火                                    |
| 20  | CONFLICT / PENDING の common_user を本人照合に使わない | ✅                                                 |

### 運用上の注意

- **`CLAIM_API_ENABLED` を ON にするのは、固定テストベクトルが両システムで
  一致したあと。** 先に ON にしても、相手の署名が旧形式なら全部 403 で落ちる。
  そのとき本システムから見えるのは「署名の合わない要求が来ている」だけで、
  **攻撃と設定漏れの区別がつかない。**
- 鍵の入れ替えは `CLAIM_HMAC_KEYS` に新旧を併記してから切り替える。
  1 本だけにすると、差し替えた瞬間に相手の要求が全部落ちる。

### 2026-08-14 追加分（完成指示書 §4・§5）

| #   | 検査項目                                           | 結果                        |
| --- | -------------------------------------------------- | --------------------------- |
| 21  | `Idempotency-Key` が無ければ `400`                 | ✅                          |
| 22  | 同一キー・同一内容の再送は最初の結果を返す         | ✅                          |
| 23  | 同一キー・異なる内容は `409 IDEMPOTENCY_CONFLICT`  | ✅                          |
| 24  | 8 本同時でも受取の成立は 1 回だけ                  | ✅                          |
| 25  | **知らないトークンで冪等キーを使い潰さない**       | ✅                          |
| 26  | **保留は記録せず、解決後に同じキーで再実行できる** | ✅                          |
| 27  | `X-Correlation-Id` を受け取り応答へ返す            | ✅                          |
| 28  | 未指定なら発番する                                 | ✅                          |
| 29  | 改行・長すぎる値を採用しない                       | ✅ ログの行を偽装させない   |
| 30  | 署名に失敗した要求にも相関IDが付く                 | ✅ 手前で落ちた要求も追える |

### 積み残し

- **Wallet への配送そのものは未実装**（PR-NW04）。本 PR で確定した受取権は
  `wallet_delivery_status = 'pending'` に載るだけで、誰も掃き出さない。
  そのため公開状態は `DELIVERED` へ進まない。
  配送完了の判定は**案イ**で確定（完成指示書 §20）。
  `entitlement.granted` に対する Wallet の `2xx` をもって `delivered` にする。
  別のコールバックを作らず、定期 GET でも確認しない。
- **`UD-1009` Claim Token の再発行は未実装。本番前必須。**
  平文を保存していないため、利用者が URL を失うと再表示できない。
  復旧手段が無いままでは運用できない（`SECURITY_DESIGN.md` §8.1）。
- **レート制限（`UD-601`）は実装済み**（§9-7）。台数を増やすときの注意は同節を参照。
- `UD-1004`（鍵の配布・交換手順）は未決定。実装は新旧併存に対応済み。

---

## 9-7. Claim API のレート制限（`UD-601`）検証結果

実行環境: Node.js v22.22.2 / pnpm 9.15.4 / 実行日 2026-08-14

| #   | 検査項目                               | 結果                              |
| --- | -------------------------------------- | --------------------------------- |
| 1   | 上限を超えると `429` ＋ `RATE_LIMITED` | ✅                                |
| 2   | `Retry-After` を秒で返す               | ✅ 切り上げ・最低 1 秒            |
| 3   | `GET` と `POST` が別枠                 | ✅ GET が枯れても POST は通る     |
| 4   | **署名に失敗した要求は枠を消費しない** | ✅ 締め出しに使えない             |
| 5   | 既定値が 5 秒ポーリングを妨げない      | ✅ 毎分 12 回で余裕あり           |
| 6   | 窓が変わればまた通る                   | ✅                                |
| 7   | 拒否した要求も数える                   | ✅ 超過分を受け続けない           |
| 8   | 窓の開始時刻が送信のたびに動かない     | ✅ 永久に拒否される状態を作らない |

### 設定

| 対象            | 既定値（1 分・鍵IDごと） | 環境変数                        |
| --------------- | ------------------------ | ------------------------------- |
| `GET` 状態取得  | 3000                     | `CLAIM_RATE_LIMIT_GET_PER_MIN`  |
| `POST` 受取確定 | 300                      | `CLAIM_RATE_LIMIT_POST_PER_MIN` |

### 運用上の注意

⚠️ **台数を増やすと、実効の上限が台数倍になる。**
数を覚えているのがプロセス内メモリのため、各プロセスは自分の分しか知らない。
2 台なら 2 倍まで通る。nonce や冪等キーのように「効かなくなる」のではなく
**静かに緩む**ので、**壊れていることが外から見えない。**

台数を増やすときは、次のどちらかを行う。

1. 上限値を台数で割る（`CLAIM_RATE_LIMIT_*` を変更する）
2. 共有の実装へ差し替える（`RateLimiterPort` の実装を入れ替えるだけ）

置き場所（Redis / DB）はホスティング（`UD-1101`）が決まってから選ぶ。
1 リクエストごとに共有ストアへ往復すると、その書き込み自体が負荷の増幅になるため、
台数が決まる前に先回りしない。

⚠️ **`GET` の値を実利用より小さくしない。**
OVEW Wallet の Claim 画面は `DELIVERY_PENDING` のあいだ **5 秒間隔で
ポーリングする既存実装**（1 セッションあたり毎分 12 回）。既定の 3000 なら
同時 250 セッションまで耐えるが、ここを絞ると**攻撃ではなく正規の利用者が弾かれる。**
しかも症状は「受け取り画面が進まない」であり、レート制限が原因だと気づきにくい。
値を下げるときは、同時セッション数の見積もりと突き合わせる。

⚠️ **窓の境目では短時間に上限の 2 倍まで通りうる**（固定窓のため）。
上限が実利用より桁で大きいうちは問題にならない。実利用へ近づけるときに思い出すこと。

---

## 10. 本文書の未決定事項

| ID      | 概要                                     |
| ------- | ---------------------------------------- |
| UD-1101 | ホスティング先と staging 環境の有無      |
| UD-1102 | 監視基盤・アラート通知先・オンコール体制 |
| UD-1103 | バックアップ間隔・保持期間・RPO/RTO      |

---

## 8. 購入者への知らせ（P0-4）

### 8.1 フラグ

| 環境変数                          | 役割                            | 既定   |
| --------------------------------- | ------------------------------- | ------ |
| `NOTIFICATION_GENERATION_ENABLED` | 知らせを**作る**か              | 無効   |
| `NOTIFICATION_DELIVERY_ENABLED`   | 知らせを**送る**か              | 無効   |
| `MAIL_PROVIDER`                   | 送信事業者（`none` / `resend`） | `none` |

⚠️ **1 本にまとめていない。** まとめると、送信だけ止めたい場面で生成まで
止まり、**止めていたあいだの注文が永久に知らされなくなる**。

⚠️ **生成が無効なのに送信だけ有効、という組み合わせは起動を拒否する。**
1 通も積まれないので、設定した人は「有効にした」と思ったまま気づけない。

⚠️ `"true"` の完全一致だけが有効。`"1"` や `"TRUE"` は**黙って無効へ倒さず
起動を拒否**する。

### 8.2 有効化の順序

1. マイグレーションを当てる（**コードのデプロイより先に**）
2. `NOTIFICATION_SITE_NAME` と `NOTIFICATION_SITE_URL` を配備ごとに設定する
   —— ⚠️ **`NOTIFICATION_SITE_URL` を誤ると、知らせの中のリンクが全部おかしくなる。
   送ったあとに気づいても、届いたメールは直せない。**
3. 管理画面（`/api/v1/admin/notifications/templates`）で 9 種別の文面を確認する
4. `NOTIFICATION_GENERATION_ENABLED=true`（**まだ送られない**）
5. 積まれ方を送信履歴で確認する
6. `MAIL_PROVIDER` / `RESEND_API_KEY` / `MAIL_FROM_ADDRESS` /
   `SUPABASE_SERVICE_ROLE_KEY` を設定する
7. ステージングで `NOTIFICATION_DELIVERY_ENABLED=true` にし、実際に受信箱へ
   届くことを確かめる
8. 本番で有効化する

### 8.3 止め方

| 段  | 操作                                    | 効果                                                                        |
| --- | --------------------------------------- | --------------------------------------------------------------------------- |
| 1   | `NOTIFICATION_DELIVERY_ENABLED=false`   | 送信のみ停止。**積まれ続けるので、復旧後にまとめて送れる**                  |
| 2   | `NOTIFICATION_GENERATION_ENABLED=false` | 生成も停止。⚠️ **止めていたあいだの分は積まれない**（あとから補完できない） |
| 3   | コードを revert                         | 上で足りない場合のみ                                                        |

⚠️ **段 2 は段 1 より重い。** 送信を止めるのは延期だが、生成を止めるのは
取りこぼしになる。まず段 1 で様子を見る。

### 8.4 やらないこと

- ⚠️ **送信履歴の行を消さない。** 「送ったつもり」を検証できなくなる
- ⚠️ **公開した文面の版を書き換えない。** 直すときは新しい版を作る
- ⚠️ **`SUPABASE_SERVICE_ROLE_KEY` を画面側（`apps/web`）へ渡さない。**
  全利用者を読める鍵で、この用途以外へ配らない

---

## 9. 運営ダッシュボード（P0-6）

**朝いちばんに `/admin` を開く。** 見るのはひとつ、「手当てが要ること」の
欄が空かどうかだけ。空なら、その日は何もしなくてよい。

### 9.1 赤（要対応）の 4 つと、その場でやること

| 赤くなる条件             | 何が起きているか                               | やること                                                  |
| ------------------------ | ---------------------------------------------- | --------------------------------------------------------- |
| 受取権の発行を打ち切り   | **お金を受け取ったのに、お渡しするものが無い** | `/admin/entitlements` から注文を開き、発行し直す          |
| 人の確認を待っている     | 機械が決められなかった（返金の宛先不明など）   | 確認事項の一覧で対応し、印を付ける                        |
| 知らせの送信を打ち切り   | 買った方が状況を知る手立てを失っている         | `/admin/notifications` で理由を確かめ、直してから送り直す |
| 時計が長く成功していない | 上の 3 つが**増え続ける**                      | cron の設定・API の生死を確かめる。止まった原因を先に直す |

⚠️ **時計の停止がいちばん先。** 止まっているあいだ、発行もお届けも知らせも
溜まる。溜まった側は「待ち」としか出ないので、**ここが唯一の気づき口**である。

### 9.2 黄色（確認）——今日中でよいもの

- **ウォレットへのお届けを打ち切り** — 相手側の障害であることが多い。
  復旧を待ってから `/admin/wallet-deliveries` で送り直す
- **外部サービスの接続確認の失敗** — `/admin/integrations` で接続先と資格情報を確かめる
- **決済の知らせが長く届いていない** — ⚠️ **ご注文が無いだけかもしれない。**
  売れていない日と壊れている日は、この画面からは区別できない
- **時計がまだ一度も成功していない** — 繋ぐ前ならこれが正常

### 9.3 「待ち」は赤にならない

発行待ち・お届け待ち・送信待ちは、件数がいくら多くても色が付かない。
仕組みが動いている証拠で、次の巡回で減るからである。

⚠️ **これを赤にしてほしいという要望が出たら、断る理由をここで思い出すこと。**
毎朝赤が出る画面では、運営は赤を見なくなる。**赤が意味を持つのは、赤でない
ときがある場合だけ**である。

### 9.4 記録の食い違い（`/admin/consistency`）

5 種類を数える。**この画面は直さない。** 見つけた機械が黙って直すと、なぜ
食い違ったのかが分からないまま同じことが繰り返される。

| 調べること                         | 見つかったら                                                 |
| ---------------------------------- | ------------------------------------------------------------ |
| お支払い済みなのに受取権が足りない | 受取権の一覧から発行し直す。時計が止まっている可能性もある   |
| 発行済み数と受取権の実数が合わない | **自動で直さない。** 原因を特定してから手当てする            |
| 取り消したのにウォレットへ未通知   | 取消の知らせの補完（`reconcile-revocations`）を実行する      |
| お受け取り済みで配送の記録が無い   | Wallet 連携より前のぶんなら正常。有効化後に増えたら調べる    |
| 伏せていない宛先が残っている       | **至急。** DB の制約を迂回した書き込み経路がある（`UD-503`） |

⚠️ **0 件の項目も出る。** 消していないのは、「調べたのか、調べていないのか」を
区別できるようにするため。

### 9.5 やってはいけないこと

- ⚠️ **金額を直接書き換えない。** 口を作っていない（P0/P1 §9.3）
- ⚠️ **`paid` へ手で変えない。** 決済の確定は Webhook だけが行う
- ⚠️ **注文・決済のデータを消さない**
- ⚠️ **記録の残らない状態変更をしない。** やり直しの操作はすべて監査に残る

### 9.6 時計仕掛けの記録（`job_runs`）

種別ごとに 1 行だけ持ち、cron が通るたびに上書きする。

⚠️ **失敗しても `last_succeeded_at` は消えない。** 消すと「最後にいつ
成功したか」——見たい値そのもの——が失われる。失敗は `last_failed_at` に別で残る。

⚠️ **合言葉（`X-Internal-Job-Token`）を通っていない呼び出しは記録しない。**
記録できると、合言葉を知らない誰かが叩くだけで「時計は動いている」ことにできる。

### 9.7 作品審査待ちについて

指示書は「作品審査待ち」を指標に挙げているが、**この仕組みには審査という
状態が無い**（`draft` / `published` / `archived` の 3 つのみ。`UD-102` で
審査を戻さないと決めている）。**無い状態を数えて常に 0 を並べると、
「異常が無い」と読まれる。** 数えていないことを、数えているふりにしない。

---

## 10. 本番販売を始める（P0-7）

**売れる状態と、売ってよい状態は別である。** 10 条件がそろうまで、本番の
お支払い口は作られません（`/admin/production` で状況が見えます）。

### 10.1 順序

条件どうしに依存があるので、この順で埋めるといちばん短く済みます。

| 順  | やること                                                        | 埋まる条件         |
| --- | --------------------------------------------------------------- | ------------------ |
| 1   | 本番の決済の鍵を取り込み、接続確認を通して有効化                | 決済の鍵           |
| 2   | 決済事業者の管理画面で送信先を設定し、試験用の知らせを 1 通送る | Webhook の署名     |
| 3   | オーナーが手数料率を承認                                        | 手数料率           |
| 4   | 規約・プライバシー・特商法を公開し、施行日を迎える              | 法務文書 3 つ      |
| 5   | 外部サービスの画面で OVEW Wallet の接続確認                     | Wallet の接続確認  |
| 6   | `/admin/production` でメールの試し送り                          | メールの接続確認   |
| 7   | 時計（cron）を 1 巡させる                                       | 発行・お届けの時計 |
| 8   | オーナーが認証基盤で二要素を登録し、**二要素で入り直す**        | 二要素認証         |
| 9   | 本番の鍵で 1 件購入し、お届けまで通して結果を記録               | 通し試験           |
| 10  | 運営責任者が本番販売の開始を承認して記録                        | 責任者の承認       |

⚠️ **段 8 は「登録しただけ」では埋まりません。** 二要素で**入り直す**まで
記録が残りません。

⚠️ **段 9 と段 10 は、段 1 が終わっていないと記録できません。** どの決済の鍵に
ついての記録かを紐づけるためです。

### 10.2 気をつけること

- ⚠️ **判定は毎回やり直されます。** 一度そろっても、崩れればその時点で
  止まります。「昨日は売れたのに」は、どれかが崩れたということです
- ⚠️ **決済の鍵を替えると、通し試験と承認は失効します。** 鍵が替わるのは
  運営会社や入金先が変わるということで、前の鍵で通した試験は新しい鍵の
  証拠になりません。**替えたら両方を取り直してください**
- ⚠️ **接続確認は 30 日で古くなります。** 相手側の証明書も鍵も入れ替わります
- ⚠️ **二要素の記録は 90 日で古くなります。** 外したことはこちらからは
  分からないので、期限を切っています
- ⚠️ **staging では止まりません。** 判定はしますが、お支払い口は作れます
  （試すためです）。画面にもそう出ます

### 10.3 押した記録について

通し試験と承認の記録は、**消せません**。

- 訂正は、**新しい記録を足して**表します。失敗した記録も残るので、
  何度やり直したかが読めます
- ⚠️ **覚え書きに鍵や合言葉を書かないでください。** そのまま保存され、
  あとから消せません
- 記録できるのは**オーナーだけ**です

### 10.4 止め方

本番販売を止めたいときは、**条件を崩すのではなく、決済の連携を止めます**
（外部サービスの画面）。条件を崩す（規約を取り下げる等）と、購入者から
見える情報まで消えます。

⚠️ **`production_attestations` の行を消して止めようとしないでください。**
消せませんし、消せたとしても記録が失われるだけで、止める手段としては
いちばん筋が悪い方法です。

## 10. 顧客サポート（P1-1）

**お問い合わせを受けたら `/admin/customers` を開く。** 注文番号・共通顧客ID・
アカウントIDのいずれかで引くと、その方の状況が 1 画面にまとまる。

### 10.1 ご本人の確認

⚠️ **この仕組みはお名前もご連絡先も持っていません**（`UD-503`）。画面にも
出ません。ご本人であることは、次のどれかで確かめてください。

- ご注文の内容（注文番号・金額・日付）を照合する
- 登録済みのご連絡先へこちらから確認を送り、ご返信をいただく
- 本人確認書類を確認する（**書類は保存しない**）

### 10.2 「応対の前に」に出るもの

| 出るもの                   | 意味                                                     |
| -------------------------- | -------------------------------------------------------- |
| アカウントが停止中（赤）   | ログインできない・買えないというお問い合わせの答えがこれ |
| お受け取りがお済みでない   | 受取のご案内が届いていない可能性                         |
| ウォレットへのお届けが未了 | 受け取り済みなのに届いていない。送り直せる               |
| 共通顧客IDが未解決         | ウォレットへ届けられない。解決の巡回を待つ               |
| ご返金の手続きが進行中     | 事業者からの結果待ち                                     |

### 10.3 手当て

**この画面には救済のボタンを置いていません。** それぞれの画面へのリンクが
あります。同じ操作を 2 か所に作ると、規則が 2 か所に散るためです。

- 受取権の発行し直し・その方ぶんの再配送 → `/admin/entitlements`
- お届けの理由の確認と送り直し → `/admin/wallet-deliveries`
- ご返金 → `/admin/orders` の注文詳細

### 10.4 ご連絡先の変更

⚠️ **この画面ではご連絡先そのものは変わりません。** 手順は 3 段です。

1. お申し出を記録する（新しいご連絡先は**伏せた表記だけ**が残ります）
2. **本人確認を記録する**（お名前が記録に残ります）
3. **認証基盤側で変更してから**、「変更済み」として記録する

⚠️ **2 を飛ばして 3 はできません**（API と DB の両方が断ります）。
⚠️ **3 の順序を逆にしないでください。** 記録してから変えると、変え忘れが
記録の中に埋もれます。

見送るときは理由が必須です。理由の無い見送りは、同じ方から次に
お問い合わせが来たときに何の役にも立ちません。

### 10.5 同じ方かもしれないアカウント

手がかり（ご連絡先の一致・共通顧客IDの一致）が合うアカウントを並べます。

⚠️ **一致しただけで、同じ方とは限りません。** 共用のアドレス、引き継いだ
アドレス、代理店システム側の取り違え——いずれもありえます。

⚠️ **この画面から統合することはできません。** 統合の口は作っていません。
必要な場合は、本人確認を経たうえで別の手続きとして検討してください。

### 10.6 やってはいけないこと

- ⚠️ **本人確認なしに、注文・受取権・ウォレットの持ち主を変えない。**
  そもそも口がありません（指示書 §11）
- ⚠️ **申し送りを「消したつもり」にしない。** 消せません。訂正は書き足す
- ⚠️ **お客さまの一覧をただ眺めない。** 条件無しの一覧は作っていません

---

## 11. 作家さまからのお問い合わせ（P1-2）

**「今月いくらですか」に、運営が DB を引いて答えない。** 作家さまご自身が
`/creator/earnings` で見られます。まずそちらをご案内してください。

### 11.1 「見込みと違う」と言われたら

見込みと締めた実額は**同じ計算**で出しています（`PayoutService.estimateFor`
が、締めるときと同じ `buildFor` を呼びます）。それでも額が動く理由は 2 つです。

| 動く理由       | 説明                                                     |
| -------------- | -------------------------------------------------------- |
| ご返金があった | 返金の窓が閉じるまで、見込みから引かれる可能性が残ります |
| 繰越が乗った   | 前の月が最低支払額に満たなければ、その分が今月へ乗ります |

⚠️ **「システムの不具合ですね」と即答しないでください。** まず
`/admin/payouts` でその期間の明細を開き、返金の行（差し戻し）があるかを
確かめます。差し戻しがあれば、それが差額です。

### 11.2 「振込先はどこで登録するのですか」

**まだ登録できません（P1-3）。** 画面にも「準備中」と出ています。
「登録する場所が見つからない」のではなく、まだ用意していません。
そのようにお伝えください。⚠️ **メールなどで口座情報を送っていただかない
でください。** 受け取っても入れる場所がなく、手元に残るだけになります。

### 11.3 「CSV が文字化けします」

BOM は付けて返しています。それでも化ける場合は、表計算ソフトが「テキスト
ファイルウィザード」経由で開かれている可能性があります。ファイルを
ダブルクリックで開いていただくようご案内してください。

⚠️ **CSV に、お買い上げくださった方のお名前もご連絡先も入っていません。**
「購入者名簿が欲しい」というご要望には応じられません。仕組みとして持って
いません（`UD-503`）。

### 11.4 「作品の審査はどうなっていますか」

**この仕組みに作品審査はありません**（`UD-102`）。公開すればそのまま
店先に並びます。⚠️ **「審査中です」と答えないでください。** 待たせても
何も起きません。

### 11.5 やってはいけないこと

- ⚠️ **作家さまの売上を、運営が代わりに書き換えない。** 口がありません
  （`SETTLEMENT_AND_REFUND.md` §4）
- ⚠️ **`/api/v1/creator/**` に「誰の分か」を渡す口を足さない。** 足した
  瞬間に、そこが他人の商いを覗く道になります
- ⚠️ **販売規約への同意を、ログイン時の同意へ束ねない。** 買うだけの方に
  販売規約を承諾させることになります
